# Schemas

JSON/Zod schemas for prompt templates, workflow configurations, and agent definitions.

## Planned Schemas

- `prompt-template.schema.json` — schema for .ai/prompts/ files
- `workflow.schema.json` — schema for .ai/workflows/ files
- `agent.schema.json` — schema for .ai/agents/ files
- `issue-request.schema.json` — schema for issue-based feature requests
