# AI Memory Store

This directory stores persistent memory for AI agents across sessions.

## Structure

- `decisions/` — architectural decision records
- `lessons/` — lessons learned during development
- `patterns/` — reusable patterns and idioms discovered in the codebase
- `config/` — agent configuration and preferences
