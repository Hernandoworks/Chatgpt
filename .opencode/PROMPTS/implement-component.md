Read every file under .opencode/.

Read the component specification.

Follow all project standards.

Implement the component completely using COMPONENT_TEMPLATE.md.

Run lint and tests.

Fix any issues.

Update documentation if needed.

Create a single commit using the required commit format.

Stop after the task is complete.
