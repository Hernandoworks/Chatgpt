Read every file under .opencode/.

Read the testing specification.

Write tests covering:

- Render
- Props
- Variants
- States (loading, disabled, empty, error)
- Events
- Edge cases

Run tests.

Fix any issues.

Create a single commit using the required commit format.

Stop after the task is complete.
